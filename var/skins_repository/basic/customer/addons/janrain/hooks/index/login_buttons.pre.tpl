{if $addons.janrain.appdomain}
<div class="janrain-wrap">
	<span>{$lang.or}</span>{include file="buttons/button.tpl" but_role="text" but_meta="janrainEngage nobg" but_text=$lang.janrain_social_login}
</div>
{/if}